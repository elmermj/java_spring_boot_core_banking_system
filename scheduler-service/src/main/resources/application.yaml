spring:
  application:
    name: scheduler-service

  datasource:
    url: jdbc:postgresql://localhost:5432/jsb_cbs_unified?currentSchema=scheduler_service
    username: jsb
    password: jsb

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        default_schema: scheduler_service

  flyway:
    enabled: true
    schemas: scheduler_service
    default-schema: scheduler_service

  quartz:
    job-store-type: jdbc
    jdbc:
      initialize-schema: never
    properties:
      org.quartz.threadPool.threadCount: 2
      org.quartz.jobStore.acquireTriggersWithinLock: true

  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8086

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics