spring:
  application:
    name: fx-service

  datasource:
    url: jdbc:postgresql://localhost:5432/jsb_cbs_fx
    username: jsb
    password: jsb

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true

  cache:
    type: simple

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: fx-service
      auto-offset-reset: earliest
      enable-auto-commit: false
    listener:
      ack-mode: manual

server:
  port: 8085

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics